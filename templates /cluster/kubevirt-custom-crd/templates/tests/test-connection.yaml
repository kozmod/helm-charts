apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubevirt-custom-crd.fullname" . }}-test-connection"
  labels:
    {{- include "kubevirt-custom-crd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubevirt-custom-crd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
