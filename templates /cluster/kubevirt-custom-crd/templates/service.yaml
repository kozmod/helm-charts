apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubevirt-custom-crd.fullname" . }}
  labels:
    {{- include "kubevirt-custom-crd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubevirt-custom-crd.selectorLabels" . | nindent 4 }}
