#name: Release Chart
#on:
#  push:
#    branches:
#      - main
#    paths:
#      - 'Chart.yaml'
#      - 'values.yaml'
#      - 'values.schema.json'
#      - 'templates/**'
#      - 'workflows/**'
#  workflow_dispatch:
#jobs:
#  release:
#    permissions:
#      contents: read
#      packages: write
#    runs-on: ubuntu-latest
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v4
#        with:
#          fetch-depth: 0
#
#      - name: Get chart version (custom-remote-cluster)
#        id: chart_version_custom-remote-cluster
#        run: |
#          VERSION=$(grep '^version:' ./templates/cluster/custom-remote-cluster/Chart.yaml | awk '{print $2}')
#          echo "version=$VERSION" >> $GITHUB_OUTPUT
#          echo "Chart version (custom-remote-cluster): $VERSION"
#
#      - name: Chart | Push (custom-remote-cluster)
#        uses: appany/helm-oci-chart-releaser@v0.4.2
#        with:
#          name: chart_version_custom-remote-cluster
#          repository: ${{ github.repository }}
#          tag: ${{ steps.chart_version.outputs.version }}
#          path: ./templates/cluster/custom-remote-cluster
#          registry: ghcr.io
#          registry_username: ${{ github.actor }}
#          registry_password: ${{ secrets.GITHUB_TOKEN }}
#          update_dependencies: 'true'
#
#      - name: Get chart version (kubevirt-standalone-cp-gpu)
#        id: chart_version_kubevirt-standalone-cp-gpu
#        run: |
#          VERSION=$(grep '^version:' ./templates/cluster/kubevirt-standalone-cp-gpu/Chart.yaml | awk '{print $2}')
#          echo "version=$VERSION" >> $GITHUB_OUTPUT
#          echo "Chart version (kubevirt-standalone-cp-gpu): $VERSION"
#
#      - name: Chart | Push (kubevirt-standalone-cp-gpu)
#        uses: appany/helm-oci-chart-releaser@v0.4.2
#        with:
#          name: chart_version_kubevirt-standalone-cp-gpu
#          repository: ${{ github.repository }}
#          tag: ${{ steps.chart_version.outputs.version }}
#          path: ./templates/cluster/kubevirt-standalone-cp-gpu
#          registry: ghcr.io
#          registry_username: ${{ github.actor }}
#          registry_password: ${{ secrets.GITHUB_TOKEN }}
#          update_dependencies: 'true'
#
#
